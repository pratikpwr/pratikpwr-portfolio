# Public environment variables for local development and CI.
# Copy to `.env` and fill in values. Web3Forms access keys are designed
# to be public (browser-submitted), but keeping them in env makes rotation easy.

PUBLIC_WEB3FORMS_ACCESS_KEY=your-web3forms-access-key
